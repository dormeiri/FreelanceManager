<Window x:Class="FreelanceManager.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FreelanceManager.Desktop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <Label>Loaded Bill: </Label>
                <Label Name="LabelLoadedBill"></Label>
            </StackPanel>
            <Button Name="BtnProjects" Click="BtnAddProject_Click">Projects</Button>
            <Button Name="BtnBills" Click="BtnBills_Click">Bills</Button>
        </StackPanel>
        
        <Frame Name="FrameOpt" Grid.Column="1"/>
    </Grid>
</Window>
